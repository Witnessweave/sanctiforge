#!/bin/bash
# LIFEQUEST QUICK COMMAND
# Usage: lq log "description" category points
#        lq progress
#        lq help

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/lifequest.py" "$@"
